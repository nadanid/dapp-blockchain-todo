pragma solidity ^0.5.0;

contract TodoList {

    // taskCount scope: entire smart contract 
    uint public taskCount = 0; //state variable written to storage on the blockchain - changes smart contract state

    //public creates a taskCount () that allows accesss to variable outside of smart contract 
}
    
